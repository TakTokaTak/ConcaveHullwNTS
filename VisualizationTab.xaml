<UserControl x:Class="ConcaveHullwNTS.VisualizationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="0,0,0,18">
    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Область визуализации -->

            <Canvas x:Name="MainCanvas" Background="#FFEBEDD8" ClipToBounds="True"></Canvas>


            <!-- Панель инструментов внизу -->
    <Border Grid.Row="1" Background="LightGray" BorderBrush="DarkGray" BorderThickness="0,1,0,0" Padding="5">
        <StackPanel Orientation="Horizontal">
                <Button x:Name="SaveResultButton_VisualizationTab" Content="Сохранить результат" Padding="10,5" Click="SaveResultButton_Click"/>
            </StackPanel>
    </Border>
    </Grid>
</UserControl>